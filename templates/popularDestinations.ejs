<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Popular Destinations</title>

    <link rel="stylesheet" href="/style.css">
<body>
    <h1 style="text-align:center"><u>Popular Destinations</u></h1>
    <p style="text-align: center;"><a href="/">HOME</a></p>
    <style>
      .popDestDiv{
        padding:20px;
        display:flex;
        max-width: 1500px;
      }
      .destinationInfo{
        display:flex;
        flex-wrap:wrap;
        justify-content: center;
        gap:20.5px;
      }
      #destinationCard{
        width:300px;
        height:180px;
        border:0.71px solid black;
        margin:10px;
        text-align:center
      }

      #destinationCard img{
        width:100%;
        height:195px;
        object-fit: cover;
      }


    </style>

    <div class="popDestDiv">
        <div class="destinationInfo">
            <% destinations.forEach(elem=>{ %>
                <div id="destinationCard">
                   <b>Destination Name:</b> <%= elem.location %>
                   <br>
                   <img src="<%= elem.image %>" alt="Image of a city's location" width="300"/>
                   <br>
                </div>
            <% }); %>
        </div>
    </div>

</body>
</html>
