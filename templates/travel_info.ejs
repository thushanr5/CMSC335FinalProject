<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style_hp.css" type="text/css">
    <!--CAN WE GIVE THIS OUR OWN NAME-->
    <title>Fights Form</title>
</head>
<body>
    <div class="bg_img">
        <div class="margin center_txt">

            <div class="design">

                <h1>Flights:</h1>
                <div class="flight_img">

                    <!-- <h1 class="">Flights:</h1> -->
                    <!-- <img src="/images/flight_in_beach.png" alt="Image" id="displayedImage" class="flight_in_beach_att"> -->

                    <br><br><br><br><br><br><br><br>
                    <br><br><br><br><br><br><br><br>


                    <!-- Not being used
                    <form action="/get_flights" method="post">
                        <fieldset class="form_color">
                          <legend><strong>Departure Airport</strong></legend>
                          <% departureChoices.forEach(option => { %>
                            <div class="radio-option">
                              <input 
                                type="radio" 
                                id="dep-<%= option %>" 
                                name="selectedDep" 
                                value="<%= option %>"
                                <%= departureChoices[0] === option ? 'checked' : '' %>
                              >
                              <label for="dep-<%= option %>"><%= option %></label>
                            </div>
                          <% }); %>
                        </fieldset>
                      
                        <fieldset class="form_color">
                          <legend><strong>Arrival Airport</strong></legend>
                          <% arrivalChoices.forEach(option => { %>
                            <div class="radio-option">
                              <input 
                                type="radio" 
                                id="arr-<%= option %>" 
                                name="selectedArr" 
                                value="<%= option %>"
                                <%= arrivalChoices[0] === option ? 'checked' : '' %>
                              >
                              <label for="arr-<%= option %>"><%= option %></label>
                            </div>
                          <% }); %>
                        </fieldset>
                      
                        <div class="center_txt">
                          <input type="reset" value="Clear" class="button_att"/>
                          <input type="submit" value="Submit" class="button_att"/>
                        </div>
                      </form> -->

                    <form action="/get_flights" method="post">
                        <fieldset class="form_color">
                            <!-- The user will enter either a ICAO or ITIA
                            <label><strong>Departure: </strong><input type="text" name="departure" required></label>
                            <span> -- </span>
                            <label><strong>Arrival: </strong><input type="text" name="arrival" required></label> -->

                            <label for="depSelect"><strong>Departure IATA:</strong></label>
                            <select id="depSelect" name="selectedDep">
                                <% departureChoices.forEach(option => { %>
                                    <option value="<%= option %>"><%= option %></option>
                                <% }); %>
                            </select>

                            <span>&emsp;&emsp;</span>
                            
                            <label for="arrSelect"><strong>Arrival IATA:</strong></label>
                            <select id="arrSelect" name="selectedArr">
                                <% arrivalChoices.forEach(option => { %>
                                    <option value="<%= option %>"><%= option %></option>
                                <% }); %>
                            </select>

                        </fieldset>

                        <br>
                        <div class="center_txt">
                            <input type="reset" value="Clear" class="button_att"/>
                            <span>&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;</span>
                            <input type="submit" value="Submit" class="button_att"/>
                        </div>
                    </form>

                </div>

                <!-- <p class=""> We are focused on providing you... </p> -->

                <h2 class=""> <a href="/">Home</a></h2>

            </div>

        </div>

    </div>

</body>
</html>